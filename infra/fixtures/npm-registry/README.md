# Fake npm Registry Fixture Strategy

Phase 1A fixture registry will serve deterministic packuments and tarballs from `testdata/npm`. It must include signed, unsigned, bad-signature, new-version, explicit-version, and tarball URL cases before npm adapter tasks can be marked complete.