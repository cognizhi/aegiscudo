# Fake PyPI Simple API Fixture Strategy

Phase 1A fixture registry will serve deterministic PEP 503 HTML and PEP 691 JSON pages from `testdata/pypi`. It must include allowed, quarantined, provenance-pass, provenance-fail, and mismatched-subject cases before PyPI adapter tasks can be marked complete.