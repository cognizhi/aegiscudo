# Canary Access Sandbox Fixture

This fixture is deterministic telemetry for Emergency Room tests. It uses fake token names and fake AI agent configuration paths only; it does not contain real secrets and must never perform live network access.
