# Feed Harvester

Phase 1A service placeholder. The implementation language decision is Python so the service can reuse the existing FastAPI, httpx, tenacity, and structlog stack for quota-aware feed adapters and runtime credential reload handling.

Blocked to complete full Phase 1A feed ingestion until credentials, feed quotas, and snapshot retention policy are approved.