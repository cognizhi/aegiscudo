# Phase 2 Ecosystem And Compliance Expansion Plan

Source PRD sections: Phase 2 ecosystem integrations, Feature 4.4, Feature 4.5, Feature 6 Phase 2, 4.10, 4.11, deferred gaps in Revision History.

Goal: expand beyond npm/PyPI MVP into SBOM/VEX, Cargo, Maven, richer intelligence feeds, GitHub Actions integrity scanning, and deeper compliance workflows while preserving the MVP control-plane safety model.

## Phase Status

- [x] Phase 2 has an owner: `Aegiscudo Tech Lead`.
- [ ] MVP alpha readiness gates are complete or approved for parallel Phase 2 work.
- [ ] Phase 2 scope is confirmed against the latest PRD.
- [ ] Phase 2 exit review is complete.

## Exit Criteria

- [x] SBOM Service generates current CycloneDX JSON plus CycloneDX 1.6 and SPDX 2.3 compatibility exports.
- [ ] OpenVEX v0.2.0 documents can suppress vulnerability matches with audit evidence and expiry.
- [x] deps.dev and OpenSSF Scorecard are integrated as policy signals.
- [x] Cross-ecosystem IOC correlation exists in Feed Harvester.
- [x] Cargo sparse registry proxy and `cargo-build-profile` are implemented.
- [x] Maven repository proxy and `jvm-binary-profile` are implemented.
- [ ] `aedo-cli` supports SBOM generation, attestation verification, Cargo/Maven scans, Rush scans, and GitHub Actions workflow scans.
- [ ] Dashboard exposes SBOM/VEX state and Phase 2 ecosystem evidence.

## SBOM Service

- [x] Finalize service language and architecture (Rust, axum, Postgres — `services/sbom-service/`).
- [x] Aggregate per-package SBOM fragments from Surgeon evidence.
- [x] Generate CycloneDX JSON using CycloneDX 1.7 as the current supported default profile (PRD §4.10; CycloneDX 1.7 is current as of May 2026).
- [x] Generate CycloneDX 1.6 JSON compatibility export.
- [x] Generate SPDX 2.3 JSON compatibility export.
- [x] Track SPDX 3.0 as compatibility target without blocking Phase 2.
- [x] Include purl, name, version, SHA-256 digest, ecosystem, Aegiscudo decision, and decision timestamp per component for inputs that carry resolvable integrity data; Maven `dependency:tree` input remains graph-only until checksum resolution lands.
- [x] Preserve dependency relationship graph (CycloneDX `dependencies` section; SPDX `relationships` section).
- [x] Store SBOMs in object storage with versioned key per tenant and scan (local filesystem, `file://` URI pattern matching Surgeon; remote S3/MinIO is future work).
- [x] Serve SBOM documents through API (`POST /v1/sbom/generate`, `GET /v1/sbom/{id}`, `GET /v1/sbom/{id}/metadata`).
- [x] Support dashboard export.
- [x] Support CLI export.
- [x] Validate NTIA minimum SBOM elements.
- [x] Add schema tests for CycloneDX and SPDX output.
- Progress 2026-05-12: `services/sbom-service` bootstrapped. Generates CycloneDX 1.7, 1.6 and SPDX 2.3 JSON. Stores documents to local filesystem under a digest-addressed per-tenant path. Persists metadata to `sbom_documents` table (migration 0011). Exposes generate/retrieve/metadata API endpoints.
- Progress 2026-05-12: SBOM service component metadata now matches CLI conventions more closely: each component carries explicit ecosystem metadata, integrity metadata, Aegiscudo decision metadata, SPDX `supplier` / `comment` fields, and CycloneDX root/component properties. Request validation now rejects purl/ecosystem/name/namespace/version mismatches so explicit component fields cannot drift from purl coordinates, and export generation backfills missing version/namespace from the purl when available. `POST /v1/sbom/generate` and `GET /v1/sbom/{id}/metadata` now return `ntia_validation` with per-field issues when required minimum elements are missing. 30 unit tests, all passing.
- Progress 2026-05-13: SPDX 3.0 is now explicitly tracked as the next non-blocking SBOM compatibility target in the SBOM service architecture docs, service README, and vendored schema provenance note. CycloneDX 1.7 remains the primary export profile, while CycloneDX 1.6 and SPDX 2.3 remain the current compatibility exports until a validated SPDX 3.0 export path lands.
- Progress 2026-05-13: `aedo sbom generate` now also accepts single-root Maven resolved dependency graphs via `--dependency-tree <path>` using `mvn dependency:tree` output. The SBOM test suite now validates npm (`package-lock.json` and `pnpm-lock.yaml`), PyPI (`requirements.txt`), Cargo (`Cargo.lock`), and single-root Maven resolved-graph inputs against the official CycloneDX 1.7, CycloneDX 1.6, and SPDX 2.3 schemas while also asserting preserved dependency relationships for the Maven graph path, ignoring warning/error/debug preamble noise, dropping conflict-elided verbose entries, and preserving duplicate-elided `(*)` edges without inventing a repeated subtree. Because `dependency:tree` output carries coordinates and edges but not artifact digests, this Maven path is currently graph-only and does not yet emit per-component SHA-256 values. Multi-module reactor output, non-jar dependency types, and classifier-bearing dependencies are currently rejected until the CLI grows an explicit aggregate-root model and lossless Maven identity support instead of silently merging or collapsing those inputs.
- Blocker 2026-05-13: Maven `dependency:tree` SBOM input still lacks per-component SHA-256 digests because the tree output contains resolved coordinates and edges but no artifact checksum data. Next action: join resolved Maven components against repository metadata or another checksum-bearing artifact-resolution input before promoting Maven graph inputs to digest-complete SBOM coverage.
- Progress 2026-05-14: Surgeon now persists a minimal per-package SBOM fragment alongside static-analysis reports and artifact manifests. Migration `0019_analysis_sbom_fragments.sql` adds `analysis_sbom_fragments`, and `services/surgeon/src/worker.rs` now records the analyzed package root component with purl, ecosystem, namespace/version, and SHA-256 integrity keyed by `analysis_job_id` and `artifact_id`. `services/sbom-service/src/lib.rs` now treats `analysis_job_id` as a first-class SBOM input source: when inline `components` are omitted, `POST /v1/sbom/generate` loads stored fragments for that analysis job, applies the latest artifact decision when available, and generates the requested CycloneDX or SPDX document without requiring a separate caller-side component list. Validation passed with `cargo test -p surgeon package_sbom_fragment_json -- --test-threads=1`, `cargo test -p surgeon validated_static_report_json_accepts_schema_valid_report -- --test-threads=1`, `cargo test -p sbom-service validate_generate_request_ -- --test-threads=1`, `cargo test -p sbom-service stored_fragment_components_ -- --test-threads=1`, and `cargo test -p sbom-service cyclonedx_components_include_purl_and_decision -- --test-threads=1` after adding `services/sbom-service` to the Rust workspace members so its workspace-scoped dependencies resolve under the repo root.
- Follow-up 2026-05-14: stored Surgeon fragments currently describe the analyzed root package only. Ecosystem-specific dependency-edge extraction remains a separate enhancement for richer aggregated graphs where parsers can emit dependency relationships losslessly.
- Progress 2026-05-11: schema-backed export coverage now exercises npm package-lock, pnpm lockfile, requirements.txt, manifest-assisted single-package Cargo.lock, and default-member virtual-workspace Cargo.lock inputs. Cargo root discovery also covers common globbed-member virtual workspaces, including exclude-filtered members, and Cargo dependency specs without a disambiguating source retain all matching same-name same-version targets. Requirements parsing now also applies nested pip constraint files during scan and SBOM generation while collapsing conflicting duplicate requirement hashes to a single unhashed identity. Standalone Cargo.lock root inference without an adjacent manifest, advanced non-local editable-path semantics, and general multi-module Maven reactor graph coverage remain follow-up work.
- Progress 2026-05-13: unnamed local editable Python requirements such as `-e .` and `--editable ./pkg` now derive package identities from adjacent `pyproject.toml` or `setup.cfg` metadata in both `aedo scan pypi` and `aedo sbom generate`. Local editable paths no longer require `#egg=` when the target project exposes standard Python project metadata.
- Progress 2026-05-14: Dashboard SBOM export is now implemented end to end. `sbom-service` exposes tenant-scoped list and download routes for stored documents, `aegiscudo-api` publishes tenant-scoped list/download routes behind actor-plus-tenant authorization and streams the download body without buffering it into bytes first, OpenAPI and generated shared types now include the published SBOM list/download contract and attachment headers, and Command Center adds an SBOM export panel under Analysis/Evidence with recent-document NTIA status plus JSON download. The Next proxy preserves the selected actor header and download headers; list summaries degrade corrupt or missing stored objects into `ntia_validation.valid = false` with an explanatory issue instead of failing the entire panel. Validation passed with `cargo test -p sbom-service resolve_sbom_list_limit_ -- --test-threads=1`, `cargo test -p sbom-service sbom_download_filename_sanitizes_source_and_format -- --test-threads=1`, `cargo test -p sbom-service ntia_validation_for_summary_degrades_storage_failures -- --test-threads=1`, `cargo test -p aegiscudo-api tenant_sbom_ -- --test-threads=1` (compiled with 2 ignored DB-backed route tests), `pnpm openapi:generate`, `pnpm --filter @aegiscudo/shared-types build`, `pnpm --filter @aegiscudo/command-center test -- test/control-plane-routes.test.ts test/control-plane-client.test.ts test/sbom-exports-panel.test.tsx`, and `pnpm --filter @aegiscudo/command-center typecheck`.
- [x] Add integration tests from resolved npm, PyPI, Cargo, and Maven dependency graphs.

## OpenVEX Support

- [x] Accept tenant-provided OpenVEX v0.2.0 documents through API.
- [x] Accept OpenVEX documents through CLI.
- [x] Store OpenVEX documents with tenant, source, digest, import time, and expiry policy.
- [ ] Match VEX statements against affected components.
- [ ] Suppress vulnerability matches for `fixed` status where applicable.
- [ ] Suppress vulnerability matches for `not_affected` status where applicable.
- [ ] Track `under_investigation` status distinctly from fixed/not affected.
- [ ] Record VEX-suppressed vulnerabilities in audit log with VEX document reference.
- [ ] Display VEX suppression state in dashboard vulnerability views.
- [ ] Include VEX suppression in policy simulator replay.
- [ ] Re-evaluate suppressed vulnerabilities after configurable period.
- [x] Add tests for expired VEX, malformed VEX, tenant isolation, and OpenVEX statement-history semantics.
- Progress 2026-05-12: `aegiscudo-api` now exposes tenant-scoped `openvex-documents` create/list/get routes backed by migration `0013_openvex_documents.sql`. Imported OpenVEX v0.2.0 documents are validated for required top-level identity fields and statement structure, stored with raw JSON plus digest/import metadata, and normalized into `openvex_statements` rows keyed by tenant, vulnerability, and product ID for later suppression lookup. Audit events are emitted on import. Focused `aegiscudo-api` OpenVEX validation passed with 3 unit tests green and 3 migrated-DB route tests compiled as ignored coverage.
- Progress 2026-05-13: OpenVEX coverage now adds explicit tests for expired expiry policies, malformed route payloads, list-route tenant isolation, and same-target statement history within a document. The validator continues to reject duplicate product IDs within a single statement, while explicitly allowing repeated vulnerability/product pairs across different statements so valid OpenVEX impact history remains representable. Focused validator coverage now passes with 9 OpenVEX unit tests green. Earlier local validation for this slice also reported `cargo test -p aegiscudo-api --lib -- --test-threads=1` at 25 passed, 0 failed, 32 ignored. The new migrated-DB route tests compile as ignored coverage but could not execute in the current environment because the contract database timed out (`PoolTimedOut`).
- Progress 2026-05-12: `aedo-cli` now supports `aedo vex import --file <openvex.json> --tenant-id <uuid> --actor-id <uuid> [--source <label>] [--expires-at <rfc3339>]`, reusing the saved API config and bearer token while sending the required actor header for the privileged control-plane route. Focused CLI validation passed with 2 OpenVEX import tests green.
- Progress 2026-05-12: Verified the importer against the current upstream OpenVEX release, which is still v0.2.0. Validation now enforces current statement rules (`not_affected` requires `justification` or `impact_statement`; `affected` requires `action_statement`), rejects duplicate `products[].@id` values within a statement before hitting the DB uniqueness constraint, keeps the import audit event in the same transaction as document persistence, and defaults CLI `source` to the file name instead of the full local path.
- Progress 2026-05-12: A request-time suppression prototype in `triage-counter` was reviewed and intentionally not kept. The review confirmed that joining stored OpenVEX `product_id` values to the request coordinate PURL is still unsafe with the current schema because `vulnerability_matches` rows do not identify the affected component that produced each advisory hit.
- Blocker 2026-05-12: statement matching and suppression remain blocked because stored `vulnerability_matches` rows currently carry advisory identity only, not affected component PURLs or another direct join key to OpenVEX `products[].@id` entries. Applying suppression before that join exists can over-suppress artifact findings that share an advisory across multiple components. Next action: extend vulnerability persistence or downstream evidence joins so each vulnerability match can be correlated to component identities before applying `fixed`, `not_affected`, or `under_investigation` status logic, expiry-aware suppression, and suppression audit trails.

## Expanded Feed Intelligence

- [x] Implement deps.dev package metadata ingestion (fixture-based; testdata/feeds/deps-dev.json).
- [x] Complete deps.dev versioned dependency graph ingestion end to end.
- [x] Implement deps.dev license metadata ingestion (fixture carries license field).
- [x] Implement OpenSSF Scorecard ingestion (fixture-based; testdata/feeds/openssf-scorecard.json).
- [x] Persist Scorecard score and individual checks.
- [x] Integrate Scorecard code review, branch protection, CI/CD, maintained, and signed releases signals into policy.
- [x] Implement OpenSSF Package Analysis (GCS public bucket) ingestion for behavioral pre-classification signals (fixture-based; testdata/feeds/openssf-package-analysis.json).
- Progress 2026-05-12: feed-harvester now supports env-configured live HTTP fetch for deps.dev and OpenSSF Scorecard via `FEED_HARVESTER_DEPS_DEV_URL` and `FEED_HARVESTER_OPENSSF_SCORECARD_URL`. When live fetch or payload validation fails, the harvester falls back to a valid local fixture snapshot and records the feed as `degraded`; if neither live nor fixture data is usable, the feed remains `unavailable` instead of aborting the full refresh. 16 feed-harvester tests pass, including invalid-live-payload fallback coverage.
- Progress 2026-05-12: migration `0012_feed_intelligence_records.sql` now persists normalized feed records in `deps_dev_packages`, `deps_dev_dependency_edges`, `openssf_scorecard_results`, and `openssf_scorecard_checks`. deps.dev package metadata is stored durably, dependency edges are captured from package-local dependencies, top-level edge payloads, and deps.dev node-indexed graph payloads (`nodes` with `fromNode` / `toNode`) when present, and Scorecard score plus per-check details are persisted for downstream consumers. 22 feed-harvester tests pass.
- Progress 2026-05-12: feed-harvester now accepts canonical deps.dev `GetDependencies` responses from `GET /v3/systems/{system}/packages/{name}/versions/{version}:dependencies` as valid live snapshots, counts graph-only payloads via `nodes` or explicit edges, and treats graph `nodes` as package records so normalized package and edge storage works against the versioned dependency endpoint shape. The checked-in deps.dev fixture now carries matching node-indexed graph data, and `cargo test -p feed-harvester -- --test-threads=1` passes with 26 tests.
- Progress 2026-05-12: feed-harvester now enriches live deps.dev metadata snapshots by fan-out fetching canonical `GetDependencies` graphs per package version, merging returned package identities and dependency edges into the persisted snapshot before validation. The live deps.dev contract now also aligns with triage-counter on the `deps.dev` feed name, and `cargo test -p feed-harvester -- --test-threads=1` passes with 29 tests.
- Progress 2026-05-12: triage-counter Scorecard lookup now falls back to the newest deps.dev package record that still resolves a `SOURCE_REPO` when later graph-only rows omit metadata links, preventing canonical graph snapshots from regressing the existing Scorecard policy path. Validation passed with `cargo test -p triage-counter -- --test-threads=1` (33 tests).
- Progress 2026-05-12: triage-counter now also consumes the latest deps.dev package-or-edge snapshot for the requested package version, walks the full reachable dependency graph within that snapshot, and elevates package-scoped transitive signals from `package_signal_observations` into the root request-time policy input. Existing risk vocabulary such as `known-malicious` and `install-script-detected` now propagates through harvested dependency graphs without adding a new policy contract surface. Validation passed with `cargo test -p triage-counter -- --test-threads=1` (36 tests).
- Progress 2026-05-12: triage-counter now maps package coordinates to deps.dev `SOURCE_REPO` links, derives OpenSSF Scorecard code review, branch protection, CI/CD, maintained, and signed releases signals from the latest harvested check set, and applies policy-snapshot-configured thresholds plus allow/warn/block/hitl actions at request time. Decision freshness now includes both `deps.dev` and `openssf-scorecard`, and contract consumers accept `scorecard_thresholds`. Validation passed with `cargo test -p aegiscudo-policy -- --test-threads=1`, `cargo test -p triage-counter -- --test-threads=1`, `cargo test -p aedo-cli validate_policy_file_accepts_scorecard_thresholds -- --test-threads=1`, and `pytest services/python-common/tests/test_contracts.py -q`.
- Progress 2026-05-12: triage-counter now has targeted freshness assertions for the two Phase 2 feed integrations: a stale `deps.dev` snapshot now forces bound decision freshness to `stale`, and a degraded `openssf-scorecard` snapshot now forces bound decision freshness to `degraded`. Validation passed with `cargo test -p triage-counter -- --test-threads=1` (38 tests).
- Progress 2026-05-12: migration `0014_cross_ecosystem_ioc_signal.sql` now expands `package_signal_observations` to accept `cross-ecosystem-ioc-correlation-risk`, and triage-counter binds that signal into `PolicyInput` instead of dropping it. This closes the request-time signal-name contract gap for cross-ecosystem IOC without another policy-schema change, while remaining feed-side granularity work still determines how package-wide versus artifact-scoped IOC observations should be populated. Validation passed with `cargo test -p triage-counter package_signal_observations_bind_policy_inputs -- --test-threads=1` and `cargo test -p aegiscudo-policy cross_ecosystem_ioc_placeholder_blocks_as_policy_violation -- --test-threads=1`.
- Progress 2026-05-12: migration `0015_cross_ecosystem_ioc_records.sql` now adds normalized Feed Harvester storage for cross-ecosystem IOC observations across identities, domains, IPs, URLs, package names, and behavioral fingerprints. Feed Harvester persists package-name plus optional maintainer/domain/IP/URL indicators from OpenSSF Malicious Packages and behavioral fingerprints from OpenSSF Package Analysis, while triage-counter now binds `cross_ecosystem_ioc_correlation_risk` from the latest usable IOC feed snapshots when an indicator value recurs across ecosystems. Latest-snapshot semantics now clear stale IOC matches when a newer successful IOC feed refresh contains no remaining records for that feed, while preserving the last usable IOC snapshot across `unavailable` refreshes. Request-time aggregate feed freshness now also includes `openssf-package-analysis` because IOC correlation depends on that feed. Validation passed with `cargo test -p feed-harvester ioc_records -- --test-threads=1`, `cargo test -p triage-counter cross_ecosystem_ioc_ -- --test-threads=1`, and `cargo test -p triage-counter maintainer_identity_ioc_records_bind_policy_inputs -- --test-threads=1`.
- [x] Implement cross-ecosystem IOC schema for identities, domains, IPs, URLs, package names, and behavioral fingerprints.
- [x] Correlate malicious maintainer identity across ecosystems.
- [x] Correlate destination domain/IP across sandbox reports.
- [x] Correlate behavioral fingerprint across static and dynamic evidence.
- Progress 2026-05-14: sandbox destination IOC correlation is now wired end to end. `SandboxTelemetryEvent` now carries optional `destination_url`, `destination_host`, and `destination_ip` fields in the shared Python contract and `sandbox-telemetry` schema, Emergency Room reconstructs those normalized fields for `outbound-network-attempt` events from captured collector requests, and Triage Counter now treats matching domain/IP/URL values from completed sandbox runs as cross-ecosystem IOC evidence when the same indicator recurs across the latest usable feed snapshots in more than one ecosystem. Focused validation passed with `pytest services/python-common/tests/test_contracts.py -q -k sandbox`, `pytest services/emergency-room/tests/test_emergency_room_sandbox.py -q -k "canary_exfiltration or build_script_exfiltration"`, `cargo test -p triage-counter sandbox_destination_ioc_records_bind_policy_inputs -- --test-threads=1`, and `cargo test -p triage-counter cross_ecosystem_ioc_records_bind_policy_inputs -- --test-threads=1`.
- Progress 2026-05-14: behavioral fingerprint IOC correlation is now wired at request time in Triage Counter without widening the producer contracts. The repository derives a normalized behavior vocabulary from persisted static-analysis indicators and completed sandbox telemetry, combines those components only when both static and dynamic evidence exist for the same package request, and matches the resulting canonical fingerprint against the latest usable `behavioral-fingerprint` IOC records harvested from OpenSSF Package Analysis when that fingerprint recurs across ecosystems. The initial shared vocabulary covers the current `network_access` and `exec_binary` behaviors emitted by existing static indicators plus sandbox outbound-network telemetry. Focused validation passed with `cargo test -p triage-counter behavioral_fingerprint_ioc_records_ -- --test-threads=1`, `cargo test -p triage-counter sandbox_destination_ioc_records_bind_policy_inputs -- --test-threads=1`, and `cargo test -p triage-counter cross_ecosystem_ioc_records_bind_policy_inputs -- --test-threads=1`.
- Blocker 2026-05-12: Feed Harvester freshness alerts remain blocked on missing monitoring deployment primitives in this repo. Feed Harvester already exports per-feed last-success metrics, but `infra/k8s` currently contains placeholder READMEs only and no `PrometheusRule`, `ServiceMonitor`, or equivalent alert-routing stack to wire end to end. Next action: land the monitoring-stack manifests or agreed deployment integration point, then add per-feed staleness rules against `aegiscudo_feed_last_success_timestamp_seconds`.
- [ ] Add Feed Harvester freshness alerts for every Phase 2 feed.
- [x] Add policy tests for cross-ecosystem IOC elevation.

## Cargo Support

- [x] Implement Cargo sparse registry adapter in Mosquito Net.
- [x] Support Cargo source replacement configuration docs.
- [x] Proxy Cargo registry-management API flows.
- [x] Proxy sparse index files.
- [x] Filter sparse index candidates according to policy.
- [x] Proxy non-redirecting crate downloads for registry-local default `dl` bases.
- [x] Support cross-origin or redirecting Cargo download origins.
- [x] Persist Cargo download-route signing key across Mosquito Net restarts.
- [x] Preserve Cargo.lock digest semantics.
- [x] Parse `.crate` archives in Surgeon.
- [x] Parse `Cargo.toml`.
- [x] Parse `Cargo.lock`.
- [x] Analyze feature graph.
- [x] Analyze target-specific dependencies.
- [x] Analyze build dependencies.
- [x] Analyze dev dependencies.
- [x] Analyze optional dependencies.
- [x] Analyze patch and replacement configuration.
- [x] Detect `build.rs`.
- [x] Detect procedural macro crates.
- [x] Detect vendored native code.
- [x] Detect bundled object files and precompiled libraries.
- [x] Run controlled `cargo fetch`.
- [x] Run controlled `cargo metadata`.
- [x] Run controlled `cargo tree`.
- [x] Run isolated `cargo build`.
- [x] Deny network after dependency fetch.
- [x] Diff `OUT_DIR`, project directory, cargo home, target directory, and user-home canary paths.
- [x] Inspect generated native artifacts using `strings`, `readelf`, `nm`, `ldd`, and `llvm-objdump` where available.
- [x] Demangle symbols where possible.
- [ ] Escalate suspicious native artifacts to high-fidelity detonation backlog when needed.
- [x] Add Cargo fixture registry and malicious `build.rs` tests.
- Progress 2026-05-14: Emergency Room Cargo sandbox now includes phase H (`cargo tree --locked`) between fetch (E) and build (F). `CARGO_NET_OFFLINE=true` is set for both phases H and F. `_cargo_tree_events` parses the output: a `cargo-dependency-tree` INFO event captures the full tree output (truncated at 4000 chars), and a `cargo-proc-macro-in-tree` MEDIUM event is emitted for each proc-macro crate detected in the tree. Five new unit tests cover the `_cargo_tree_events` helper; two integration tests verify that phase H appears in telemetry and that `cargo-dependency-tree` is present in events. Validation: `pytest services/emergency-room/tests/test_emergency_room_sandbox.py -x -q` — 16 passed.
- Progress 2026-05-13: Surgeon now has four dedicated `.crate` archive fixture tests exercising the `scan_artifact_package` path (as opposed to directory-only scans): a malicious `build.rs` with `TcpStream::connect` is detected via `cargo-build-script` and `rust-raw-network`, a proc-macro crate surfaces `cargo-proc-macro`, a `build = false` manifest suppresses the build-script indicator even when `build.rs` is present, and `[patch]`/`[replace]` overrides surface `cargo-patch-override`/`cargo-replace-override`. Additionally, `is_active_cargo_build_script` was extended to resolve arbitrary `build = "<filename>"` entries from `Cargo.toml` by extracting the configured filename component, so crates that set non-default build script names (e.g., `build = "compiler-plugin.rs"`) are detected correctly while the unconfigured default `build.rs` is not incorrectly flagged. Validation: `cargo test -p surgeon -- --test-threads=1` — 120 passed, 0 failed.
- Progress 2026-05-12: Surgeon now accepts `.crate` artifacts alongside `.tgz` and wheel inputs, parses `Cargo.toml` and `Cargo.lock`, flags Cargo build scripts, procedural macro crates, build-dependencies, `[patch]` / `[replace]` overrides, and git/path/alternate-registry dependency sources, and surfaces those indicators during both direct directory scans and packaged `.crate` artifact scans. Validation passed with `cargo test -p surgeon cargo_ -- --test-threads=1`, `cargo test -p surgeon rust_malicious_fixture_build_script_detected -- --test-threads=1`, and `cargo test -p surgeon -- --test-threads=1`.
- Progress 2026-05-12: Surgeon now also emits aggregate Cargo-surface indicators for feature graphs, target-specific dependency sections, dev-dependencies, and optional dependencies, so the Cargo manifest analysis reflects conditional and test-only code paths rather than only build-time and source-override signals. Validation passed with `cargo test -p surgeon cargo_ -- --test-threads=1` and `cargo test -p surgeon -- --test-threads=1`.
- Progress 2026-05-12: Surgeon now flags vendored native source files and bundled precompiled native artifacts during Cargo source and `.crate` scans. Direct directory scans ignore only top-level local `target/` build outputs for Cargo source roots, while packaged `.crate` contents under `target/` still surface as shipped package artifacts. Native-artifact classification also keeps valid Java `.class` files out of this path while still flagging spoofed `.class` filenames or shallow forged class headers. Validation passed with `cargo test -p surgeon cargo_ -- --test-threads=1`, `cargo test -p surgeon packaged_cargo_target_artifacts_are_still_flagged -- --test-threads=1`, `cargo test -p surgeon non_cargo_target_directory_is_not_ignored_for_native_detection -- --test-threads=1`, `cargo test -p surgeon valid_java_class_file_is_not_classified_as_native -- --test-threads=1`, `cargo test -p surgeon spoofed_java_header_without_valid_class_structure_is_still_classified -- --test-threads=1`, and `cargo test -p surgeon spoofed_class_extension_native_blob_is_still_classified -- --test-threads=1`.
- Progress 2026-05-12: Mosquito Net now supports an initial Cargo sparse-registry proxy slice. Cargo `config.json` is forwarded without a package decision and rewrites registry-local default `dl` bases back under the configured `/proxy/...` mount, including rooted, dot-segment, query/fragment-bearing, and same-origin absolute bases, even when valid upstream response headers are mislabelled. Encoded Cargo download-base paths are revalidated at request time, and download-route signatures now bind to an operator-held `MOSQUITO_NET_CARGO_DOWNLOAD_MAC_KEY`, so upstream sparse metadata or caller-forged proxy paths cannot steer request-time egress or credential forwarding away from the configured registry origin and previously issued `config.json` download bases remain valid across Mosquito Net restarts when that secret stays stable. Rotating that secret intentionally invalidates previously issued download bases until clients refetch `config.json`. Non-default markerized Cargo `dl` templates are also rejected until Mosquito Net can normalize them safely. Sparse index package paths normalize to Cargo package metadata requests, sparse metadata now filters blocked version entries through per-candidate artifact decisions using required `name`, `vers`, and `cksum` fields, accepts mixed-case names that canonicalize cleanly, and fails closed on missing fields, whitespace-padded names, candidate mode drift, non-outage candidate Triage failures, or malformed sparse metadata instead of silently leaving blocked versions visible. Upstream non-success sparse responses are passed through without being reparsed as candidate lists. Explicit default Cargo download paths that end in `/{crate}/{version}/download` normalize to `pkg:cargo/<name>@<version>` artifact requests that prefetch and hash upstream `.crate` bytes before Triage Counter decides. Validation passed with `cargo test -p mosquito-net cargo_registry_config_rewrite_preserves_absolute_query_and_fragment -- --test-threads=1`, `cargo test -p mosquito-net cargo_registry_config_rewrites_download_base_under_mount -- --test-threads=1`, `cargo test -p mosquito-net cargo_registry_config_rewrites_download_base_without_json_content_type -- --test-threads=1`, `cargo test -p mosquito-net cargo_registry_config_invalid_payload_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_registry_config_markerized_download_template_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_registry_config_cross_origin_absolute_download_base_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_proxy_forwards_metadata_requests -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_proxy_filters_blocked_candidates -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_proxy_filters_blocked_candidates_without_json_content_type -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_candidate_mode_mismatch_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_candidate_hard_error_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_invalid_metadata_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_missing_name_field_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_missing_version_field_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_missing_checksum_field_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_invalid_checksum_field_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_mismatched_name_field_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_invalid_present_name_field_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_mixed_case_name_field_is_allowed -- --test-threads=1`, `cargo test -p mosquito-net cargo_sparse_not_found_preserves_upstream_status -- --test-threads=1`, `cargo test -p mosquito-net cargo_download_proxy_prefetches_and_forwards_crate_bytes -- --test-threads=1`, `cargo test -p mosquito-net cargo_download_redirect_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_download_proxy_path_with_cross_origin_encoded_base_fails_closed -- --test-threads=1`, `cargo test -p mosquito-net cargo_download_route_remains_valid_after_restart_with_same_mac_key -- --test-threads=1`, `cargo test -p mosquito-net cargo_download_route_is_rejected_after_restart_with_different_mac_key -- --test-threads=1`, and `cargo test -p mosquito-net -- --test-threads=1`. Remaining Cargo proxy follow-up in this area: non-default Cargo `dl` templates plus cross-origin Cargo `api` origins.
- Progress 2026-05-13: Cargo `.crate` downloads now preserve Cargo.lock checksum semantics for registry-local default `dl` bases. Mosquito Net resolves the sparse index entry for the requested crate/version, requires the upstream `cksum` to match the exact bytes served to the client, and fails closed before Triage when the checksum mismatches or the requested version is absent from sparse metadata. Cargo download-route signatures now use HMAC-SHA256 derived from `MOSQUITO_NET_CARGO_DOWNLOAD_MAC_KEY` while preserving same-key restart stability and rotated-key invalidation behavior. Validation: `cargo test -p mosquito-net cargo_download_proxy_ -- --test-threads=1`, `cargo test -p mosquito-net cargo_download_route_ -- --test-threads=1`, and `cargo test -p mosquito-net --lib -- --test-threads=1` — 94 passed, 0 failed, 7 ignored.
- Progress 2026-05-14: `cargo-build-profile` sandbox is now implemented in Emergency Room. `SandboxProfile.CARGO_BUILD_PROFILE` runs controlled `cargo metadata` (phase D), `cargo fetch` (phase E), and `cargo build --locked` with `CARGO_NET_OFFLINE=true` (phase F) over a fresh isolated `CARGO_HOME`. `RUSTUP_HOME` is forwarded from the host environment so the rustup shim can locate its toolchain. OUT_DIR contents are reported, native binaries are inspected via `strings` and `nm`/`rustfilt`, suspicious symbols and strings flag escalation events, and canary env-var exfiltration from `build.rs` is captured by the localhost:9999 collector. `sandbox-images/cargo-runner/Dockerfile` provides the production container profile. Three new tests cover exfiltration detection, benign build phases, and unknown-profile rejection. Validation: `pytest services/emergency-room/tests/test_emergency_room_sandbox.py -k cargo` — 3 passed.
- Progress 2026-05-13: Added [Cargo Source Replacement With Mosquito Net](../development/cargo-source-replacement.md) with `.cargo/config.toml` examples that operators can apply repository-locally or in `~/.cargo/config.toml`, plus mount-path guidance, proxy validation guidance, `MOSQUITO_NET_CARGO_DOWNLOAD_MAC_KEY` rollover behavior, and the current Cargo proxy limitations. The Cargo architecture docs now link directly to that runbook.
- Progress 2026-05-13: Cargo registry-management API flows now work for explicit Cargo registry endpoints when the upstream `api` base resolves to the configured registry origin. Mosquito Net rewrites signed Cargo `api` bases under the proxied mount, forwards only allowlisted Cargo request headers plus request query and body, never falls back to the registry-config upstream credential on this path, bypasses Triage for Cargo API passthrough, and rejects forged or out-of-scope signed suffixes so the `api` base cannot become a generic same-origin proxy. Validation: `cargo test -p mosquito-net cargo_ -- --test-threads=1` and `cargo test -p mosquito-net --lib -- --test-threads=1` — 51 Cargo-focused tests passed, and the full Mosquito Net lib suite passed with 100 passed, 0 failed, 7 ignored.
- Progress 2026-05-13: Cargo live-local compatibility proofs now pass against the fixture sparse registry. The shared live proxy bootstrap now reapplies control-plane migrations before reseeding, the local fixture stack includes a dedicated Cargo sparse registry plus seeded benign `.crate` metadata, and ignored Mosquito Net proofs now validate both `cargo search` and `cargo fetch` through `/proxy/cargo-fixtures`. During this slice, triage-counter also fixed a feed-table SQL type mismatch by comparing the text-backed `deps_dev_packages.ecosystem` column against a text bind value instead of `package_ecosystem`, which had been surfacing as request-time `503` failures for Cargo sparse metadata decisions. Validation: `cargo test -p triage-counter postgres_repository_binds_cargo_request_without_deps_dev_ecosystem_cast_failure -- --ignored --test-threads=1`, `cargo test -p mosquito-net cargo_fetch_downloads_allowed_fixture_against_live_local_proxy -- --ignored --test-threads=1`, and `cargo test -p mosquito-net cargo_search_hits_live_local_proxy_api -- --ignored --test-threads=1`.
- Progress 2026-05-14: Mosquito Net now supports allowlisted cross-origin Cargo download origins and redirect targets. Migration `0018_registry_config_cargo_download_origins.sql` adds a tenant-scoped `cargo_allowed_download_origins` allowlist to `registry_configs`; Mosquito Net normalizes those origins on load, rewrites signed Cargo download bases for approved off-origin hosts, and follows redirect chains only while each hop stays on the registry origin or an allowlisted origin. Off-origin download requests never inherit the registry-config upstream credential. Focused validation passed with `cargo test -p mosquito-net cargo_registry_config_cross_origin_absolute_download_base -- --test-threads=1`, `cargo test -p mosquito-net cargo_download_redirect -- --test-threads=1`, and `cargo test -p mosquito-net registry_config::tests -- --test-threads=1`.
- Blocker 2026-05-14: cross-origin Cargo `api` bases still remain blocked by the current registry configuration contract. Mosquito Net now models allowlisted download origins only; it does not yet model approved off-origin Cargo API hosts or per-origin API credential-forwarding rules. Next action: extend the registry-config and control-plane contract for Cargo `api` origins separately, then update the signed Cargo API route machinery to validate against that allowlist instead of the current same-origin assumption.

## Maven And JVM Support

- [x] Implement Maven repository layout adapter in Mosquito Net.
- [x] Proxy POM and JAR metadata.
- [x] Preserve Maven checksums and signatures.
- [x] Parse POM dependencies, scopes, plugins, repositories, parent POMs, classifiers, and relocation.
- [x] Inspect JAR structure including manifest, signatures, service loader files, nested JARs, shaded classes, resources, and native libraries.
- [x] Verify Maven repository checksums.
- [x] Verify JAR signature metadata where available.
- [x] Disassemble bytecode using `javap -c -v` or ASM visitors.
- [x] Evaluate licensing and operational fit for CFR, FernFlower, or Ghidra optional decompilation.
- [x] Detect `Runtime.exec`.
- [x] Detect `ProcessBuilder`.
- [x] Detect socket and HTTP clients.
- [x] Detect reflection and dynamic class loading.
- [x] Detect `ClassLoader#defineClass`.
- [x] Detect deserialization APIs.
- [x] Detect filesystem and credential-path access.
- [x] Detect JNI loading.
- [x] Detect hardcoded domains, IPs, webhook URLs, and token names.
- [x] Load selected classes in isolated JVM profile.
- [ ] Capture class loading, process execution, filesystem writes, and network attempts.
- [ ] Distinguish dangerous operations from merely exposed APIs.
- [ ] Attribute behavior to bytecode, class loading, Maven plugins, or transitive dependencies.
- [x] Add Maven fixture repository and malicious plugin/JAR tests.
- Progress 2026-05-13: Mosquito Net now supports a Maven repository layout adapter (`RegistryAdapter::Maven`). Artifact requests for `.jar`, `.aar`, `.war`, and `.ear` extensions are routed through Triage Counter with `PackageRequestKind::Artifact`; POM files, `maven-metadata.xml`, and checksum files (`.md5`, `.sha1`, `.sha256`, `.sha512`, `.asc`) are forwarded as `PackageRequestKind::Metadata` without a triage hold. Maven group-id path segments are reconstructed by joining all leading path segments with dots. Short paths with fewer than 3 segments fail closed. Nine tests cover the proxy: JAR allow/block, POM metadata forwarding, maven-metadata.xml forwarding, SHA1 checksum as metadata, short-path fail-closed, aar/war/ear artifact routing, sha256/sha512/asc checksum variants, and SNAPSHOT JAR routing. Validation: `cargo test -p mosquito-net -- --test-threads=1` — 92 passed, 0 failed, 7 ignored. Remaining Maven work: POM parsing, JAR inspection, bytecode analysis, and JVM sandbox execution are all blocked pending JVM tooling integration (javap, ASM, or equivalent) in Surgeon.
- Progress 2026-05-14: Mosquito Net now verifies Maven repository sidecar checksums for artifact downloads. When a Maven JAR (or AAR/WAR/EAR) is fetched from upstream, `verify_maven_checksum` attempts to fetch the `.sha256` sidecar first, then `.sha1`, computing the corresponding hash of the artifact bytes in-process and comparing against the sidecar value. Malformed or non-hex sidecar content is skipped so accidental repo corruption does not cause false failures. If a sidecar is present and the hash mismatches, the proxy returns 502 (`MavenChecksumMismatch`) and fails closed. If no valid sidecar is available the artifact is passed through without hash enforcement. `sha1 = "0.10"` added as a workspace dependency. Three new unit tests exercise: matching SHA-1 sidecar (allowed), wrong SHA-1 sidecar (502), and no sidecar (pass-through). Three existing tests updated to tolerate additional sidecar requests in `upstream_paths`. Validation: `cargo test -p mosquito-net --lib -- --test-threads=1` — 113 passed, 0 failed, 12 ignored.
- Progress 2026-05-14: Surgeon now parses Maven `pom.xml` files structurally instead of treating them as generic text only. `services/surgeon/src/manifest.rs` emits dedicated indicators for declared dependency entries, non-compile scopes, build plugins, custom repositories, parent POM inheritance, dependency classifiers, and artifact relocation metadata. This closes the POM parsing row without depending on the still-open JVM bytecode and runtime-tooling work. Focused validation passed with `cargo test -p surgeon pom_xml_ -- --test-threads=1` and `cargo test -p surgeon cargo_toml_malformed_flagged -- --test-threads=1`.
- Progress 2026-05-14: Surgeon's existing `.class`/JAR string-extraction path now covers the remaining JVM API detection rows that do not require bytecode disassembly. `services/surgeon/src/lib.rs` now emits dedicated indicators for `Runtime.exec`, `ProcessBuilder`, raw socket clients, reflection and dynamic class loading, `defineClass`, deserialization APIs, filesystem and credential-path access, JNI loading, and hardcoded URLs/IPs/webhook or credential-token names found in compiled Java artifacts. This intentionally stays heuristic and does not close the separate `javap`/ASM disassembly row. Focused validation passed with `cargo test -p surgeon detects_extended_jvm_indicators_in_binary_file -- --test-threads=1`, `cargo test -p surgeon detects_indicators_in_binary_file -- --test-threads=1`, and `cargo test -p surgeon detects_indicators_inside_jar -- --test-threads=1`.
- Progress 2026-05-14: Surgeon now inspects JAR structure beyond raw string extraction. `services/surgeon/src/lib.rs` records manifest presence plus selected manifest attributes, service-loader registration files under `META-INF/services/`, nested archives, shaded or relocated class namespaces, embedded resource files, and bundled native-library entries while continuing to validate signature metadata separately. This closes the JAR-structure inspection row without claiming bytecode provenance or class-loading/runtime attribution. Focused validation passed with `cargo test -p surgeon detects_jar_structure_indicators -- --test-threads=1`, `cargo test -p surgeon jar_signature_metadata -- --test-threads=1`, and `cargo test -p surgeon detects_indicators_inside_jar -- --test-threads=1`.
- Progress 2026-05-14: Surgeon now verifies JAR signature metadata structure when signed archives are present. `services/surgeon/src/lib.rs` inspects `META-INF/MANIFEST.MF` plus `.SF` and signature-block (`.RSA`, `.DSA`, `.EC`) entries and emits either `jar-signature-metadata` for a complete manifest-plus-signature set or `jar-signature-metadata-incomplete` when the archive contains only partial signing metadata. This closes the metadata-verification row without claiming full certificate or trust-chain validation. Focused validation passed with `cargo test -p surgeon jar_signature_metadata -- --test-threads=1` and `cargo test -p surgeon detects_indicators_inside_jar -- --test-threads=1`.
- Progress 2026-05-14: JVM decompiler tooling evaluation is now documented in [docs/architecture/jvm-decompiler-evaluation.md](../architecture/jvm-decompiler-evaluation.md). The Phase 2 recommendation is to prefer CFR as the first optional headless decompiler for automated offline analysis, keep Fernflower as a secondary comparator or fallback, and reserve Ghidra for later high-fidelity escalation where its heavier SRE footprint is justified. The evaluation also records the current upstream license posture: CFR MIT, Fernflower Apache 2.0, and Ghidra Apache 2.0.
- Progress 2026-05-14: Surgeon packaged-artifact scanning now accepts Maven `.jar`, `.war`, and `.ear` files through the same worker path used for other archive ecosystems, instead of rejecting them as unsupported ZIP artifacts. Focused artifact regression coverage now includes the checked-in malicious Java fixture JAR plus a synthetic packaged `META-INF/maven/.../pom.xml` case that asserts build-plugin detection from embedded Maven metadata. This closes the remaining malicious Maven plugin/JAR test row now that the deterministic Maven fixture repository was already landed earlier. Validation passed with `cargo test -p surgeon scans_maven_jar_ -- --test-threads=1` and `cargo test -p surgeon scans_pypi_wheel_and_generates_manifest -- --test-threads=1`.
- Progress 2026-05-14: Surgeon now performs in-process JVM bytecode disassembly with a Rust-native classfile parser instead of relying only on printable-string extraction or an external JDK tool. `services/surgeon/src/lib.rs` parses valid `.class` files, walks method bytecode, normalizes invoke/new member references into scan text, and reuses the existing JVM indicator rules against that structured bytecode view before falling back to raw string extraction for malformed or unsupported classes. This closes the disassembly row while avoiding a `javap` dependency that would not fit the current distroless Surgeon runtime image. Focused validation passed with `cargo test -p surgeon detects_java_env_read_from_real_compiled_class_fixture -- --test-threads=1`, `cargo test -p surgeon scans_maven_jar_and_detects_malicious_fixture_indicators -- --test-threads=1`, and `cargo test -p surgeon detects_extended_jvm_indicators_in_binary_file -- --test-threads=1`.
- Progress 2026-05-14: Emergency Room now exposes an initial `jvm-binary-profile` for Maven artifacts. `services/python-common/src/aegiscudo_common/contracts.py` adds the shared profile enum, `services/emergency-room/src/emergency_room/worker.py` now maps Maven jobs onto that profile, and `services/emergency-room/src/emergency_room/sandbox.py` loads up to three selected classes from `.jar` / `.war` / `.ear` artifacts by inferring `Main-Class` from the manifest when present and otherwise falling back to discovered top-level class entries. A temporary Java source probe uses `Class.forName(..., true, ...)` under `java -verbose:class` so static initializers run inside the sandbox without requiring artifact classes to expose a `main()` entrypoint. This closes the initial isolated class-loading row and the top-level `jvm-binary-profile` exit criterion, while broader runtime attribution still remains open. Focused validation passed with `pytest services/emergency-room/tests/test_emergency_room_worker.py -q -k profile_selection_and_import_name`, `pytest services/emergency-room/tests/test_emergency_room_sandbox.py -q -k jvm_sandbox`, and `pytest services/python-common/tests/test_contracts.py -q -k sandbox`.
- Progress 2026-05-14: The same JVM sandbox path now emits `jvm-class-loaded` telemetry for selected artifact classes confirmed in `java -verbose:class` output. This advances the still-open runtime-capture row with explicit class-loading evidence while network attempts continue to flow through the existing exfiltration collector; generalized process-exec and filesystem-write tracing remain follow-up work. Focused validation passed with `pytest services/emergency-room/tests/test_emergency_room_sandbox.py -q -k jvm_sandbox`.
- Blocker 2026-05-14: the remaining JVM runtime-capture rows now depend on runner-level tracing instrumentation that does not exist in the current environment or sandbox images. Local checks confirmed `java` and `javac` are available, but `strace` and `inotifywait` are not installed, and the existing runner images do not currently include syscall or file-tracing tooling. Next action: add a dedicated JVM runner image (or equivalent sandbox base) with approved process/file tracing primitives, then wire those traces into Emergency Room telemetry before attempting to close the remaining process-execution, filesystem-write, and behavior-attribution rows.
- Progress 2026-05-13: Added a deterministic Maven fixture repository to the live-local stack. `scripts/fixture_registry.py` now serves static Maven repository layout files, Docker Compose now exposes `maven-fixture-registry` on `18083`, the local control-plane seed adds `/proxy/maven-fixtures`, and the seed script now materializes a benign Maven JAR artifact row so enforce-mode artifact requests can pass when the digest matches. Maven checksum and signature sidecars are now classified as metadata even when the base filename ends in `.jar`, and artifact-level `maven-metadata.xml` paths with multi-segment group IDs now normalize to the correct package coordinate instead of being mis-bound as versioned metadata requests. Ignored Mosquito Net live-local proofs now validate direct JAR release plus POM, `maven-metadata.xml`, checksum passthrough, and a real `mvn dependency:get` smoke fetch against the fixture repository. Validation: `cargo test -p mosquito-net maven_ -- --test-threads=1` and `cargo test -p mosquito-net maven_ -- --ignored --test-threads=1`.

## Generic HTTP Adapter

- [x] Define generic artifact capture semantics.
- [x] Implement passthrough HTTP proxy with artifact digest capture.
- [x] Apply Triage Counter hold pattern before serving captured artifacts, and fail closed when bounded capture cannot complete.
- [x] Record limited request-time protocol metadata and redacted upstream headers.
- [x] Document limitations compared with ecosystem-specific adapters.
- [x] Add tests for private/non-standard registry passthrough.
- Progress 2026-05-13: Added [Generic HTTP Adapter](../architecture/components/generic-http-adapter.md) to define the Phase 2 fallback semantics for non-standard HTTP artifact sources. The documented model limits the adapter to bounded `GET` artifact capture plus `HEAD` probe support for the same URLs, including query-bearing URLs when the query selects the artifact, SHA-256 digest computation before release, fail-closed behavior when bounded capture cannot complete, `PackageEcosystem::GenericHttp` coordinates keyed by upstream host plus normalized path, limited request-time protocol metadata handling with header redaction, and explicit lower-fidelity boundaries compared with ecosystem-specific adapters.
- Progress 2026-05-13: Generic HTTP passthrough adapter implemented in Mosquito Net. `RegistryAdapter::GenericHttp` is now proxy-supported. All GET requests are routed as `PackageRequestKind::Artifact` with a `PackageCoordinate` using `PackageEcosystem::GenericHttp`, upstream host as namespace, and normalized upstream path as name, while query-bearing source URLs are preserved in upstream fetches, artifact cache keys, and request context when they select the artifact. SHA-256 digest is computed before releasing bytes to the client. Triage Counter hold pattern enforced before serving. Sensitive upstream response headers (`authorization`, `cookie`, `set-cookie`, `www-authenticate`, `proxy-authorization`) are redacted before caching. A dedicated `proxy_head` handler supports HEAD probes for GenericHttp only (no triage call, no artifact capture); returns `NOT_IMPLEMENTED` for other adapters while still emitting audit events, request metrics, and tenant/client rate limiting like other proxy methods. HEAD responses forward `Content-Type`, `Cache-Control`, `Content-Length`, `ETag`, and `Last-Modified`, rewrite redirect `Location` headers back under the proxy mount, and preserve the caller query string when building the upstream probe URL. Tech lead review added ETag/Last-Modified forwarding, HEAD rate-limit coverage, query-preservation coverage, redirect `Location` passthrough, and an upstream non-200 HEAD probe pass-through test.

## CLI Phase 2

- [x] Implement `aedo scan cargo --lockfile Cargo.lock`.
- [x] Implement `aedo scan maven --pom pom.xml`.
- [x] Implement `aedo scan maven --dependency-tree <path>`.
- [x] Implement `aedo scan rush --config rush.json`.
- [x] Implement `aedo scan github-actions --workflow-dir .github/workflows`.
- [x] Resolve GitHub Actions tags to commit SHAs via `--resolve-tags`.
- [x] Flag mutable action tags.
- [x] Cross-reference action tag/SHA against Aegiscudo threat feed.
- [ ] Implement `aedo attest verify --lockfile package-lock.json --ecosystem npm`.
- [ ] Implement `aedo attest verify --requirements requirements.txt --ecosystem pypi`.
- [ ] Implement `aedo attest verify --lockfile Cargo.lock --ecosystem cargo`.
- [ ] Implement `aedo attest verify --pom pom.xml --ecosystem maven`.
- [x] Implement `aedo sbom generate --format cyclonedx-json`.
- [x] Implement `aedo sbom generate --format cyclonedx-1.6-json`.
- [x] Implement `aedo sbom generate --format spdx-2.3-json`.
- [x] Implement `aedo risk <package>@<version> --ecosystem npm`.
- [x] Implement `aedo risk <crate>@<version> --ecosystem cargo`.
- [x] Implement `aedo risk <group-id>:<artifact-id>@<version> --ecosystem maven`.
- [x] Note: `aedo risk` commands are Phase 2 CLI additions; the underlying Triage Counter verdict exists in MVP but the CLI subcommand is intentionally deferred to Phase 2 to keep aedo-cli MVP scope tight.
- [x] Add output and exit-code tests for every Phase 2 command.
- Progress 2026-05-12: `aedo scan cargo --lockfile`, `aedo scan maven --dependency-tree`, `aedo scan rush --config`, and `aedo scan github-actions --workflow-dir` all implemented and validated (128 passed, 0 failed). `GithubActions` added to `PackageEcosystem` in aegiscudo-core. Mutable action tag detection implemented (AllowWithWarning for non-SHA refs, Allow for 40-char hex SHA). `submit_scan_report` now skips API enrichment for non-npm/pypi ecosystems. Rush scan supports npm and pnpm lockfiles via the common/config/rush/ and common/temp/ search paths.
- Progress 2026-05-12: `aedo scan maven --pom pom.xml` implemented with `roxmltree` parser. Extracts direct compile/runtime dependencies, resolves `${property}`, `${project.version}`, and `${project.groupId}` substitutions. Skips `<dependencyManagement>` and test/system scopes. 8 new tests added. Total 140 CLI tests passing.
- Progress 2026-05-12: Tech lead review applied — Maven `first_line` root-skip now only fires when the stripped line parses as a Maven coordinate (prevents mvn preamble lines from consuming the slot); Maven namespace now set via direct `ScanFinding` construction (no more fragile `last_mut()` post-hoc mutation); `any_finding_api_enrichable` documented with single-ecosystem invariant assumption; 4 missing tests added (Maven preamble lines, Rush npm temp fallback, GitHub Actions `.yaml` extension, malformed workflow YAML). 132 tests pass, 0 failed.
- Progress 2026-05-12: `aedo scan github-actions --resolve-tags` now resolves mutable GitHub action refs through the GitHub refs API, follows nested annotated tag objects through `/git/tags/{sha}` until a commit is reached, honors optional `GITHUB_TOKEN`, and degrades back to mutable-tag warnings when resolution fails. Added targeted resolution tests, auth-header coverage, and CLI exit-code coverage. `cargo test -p aedo-cli --lib -- --test-threads=1` passed with 147 passed, 0 failed, 5 ignored; `cargo build --workspace` passed.
- Progress 2026-05-13: GitHub Actions threat-feed correlation implemented end to end. `aedo scan github-actions` now posts `githubactions` findings to `POST /v1/cli/github-actions/enrich` when a saved CLI API config exists, while config-free runs keep the prior local-only mutable-tag behavior. `aegiscudo-api` bridges the request through a dedicated non-registry query path in Triage Counter keyed by `tenant_id` plus optional `policy_profile_id`; merge logic preserves stricter local mutable-tag warnings over weaker remote `ALLOW` results; malformed owner/repo/ref coordinates are rejected; migration `0016_github_actions_package_ecosystem.sql` adds `githubactions` to the control-plane enum; OpenAPI and generated shared types now include the GitHub Actions enrich route and the existing `/v1/cli/risk` route. Focused validation passed: `cargo test -p triage-counter query_binds_policy_profile_without_registry_context -- --test-threads=1`, `cargo test -p aegiscudo-api github_actions_enrichment_route_ -- --test-threads=1`, `cargo test -p aedo-cli github_actions_ -- --test-threads=1`, `cargo test -p aedo-cli merge_scan_findings_preserves_stricter_local_decision_when_remote_allows -- --test-threads=1`, and `pnpm openapi:generate`.
- Progress 2026-05-14: GitHub Actions CLI enrichment policy context is now deterministic instead of guessing the first policy profile. `aedo auth login` and `aedo scan github-actions` now both accept optional `tenant_id` and `policy_profile_id`, saved CLI config persists those values, per-command flags override saved config when both are present, and the GitHub Actions enrich payload now carries the explicit context through to `aegiscudo-api`. The CLI fails fast when remote GitHub Actions enrichment is configured without a `policy_profile_id`, and `aegiscudo-api` now rejects that route at both request validation and shared policy-context resolution instead of falling back to the first matching profile. Focused validation passed with `cargo test -p aedo-cli github_actions_ -- --test-threads=1` and `cargo test -p aegiscudo-api github_actions_enrichment_route_ -- --test-threads=1`.
- Blocker 2026-05-12: `aedo attest verify` commands require an attestation storage and verification API layer not yet implemented in the control plane. Next action: unblock after attestation service endpoints are defined in the OpenAPI contract.
- Blocker resolved 2026-05-13: GitHub Actions threat-feed correlation no longer depends on registry-adapter assumptions. `aedo-cli` can enrich `githubactions` findings through a dedicated API route backed by Triage Counter query evaluation without requiring a registry binding.
- Blocker resolved 2026-05-14: GitHub Actions CLI enrichment no longer guesses policy context. Explicit `tenant_id` / `policy_profile_id` can now be persisted in CLI config or passed per command, per-command flags override saved config, and both the CLI and `aegiscudo-api` reject missing `policy_profile_id` on the GitHub Actions enrichment path instead of selecting the first profile.
- Blocker resolved 2026-05-12: `aedo risk` implemented. `aedo risk <spec> --ecosystem npm|pypi|cargo|maven` added to `aedo-cli`; `/v1/cli/risk` route added to `aegiscudo-api` (resolves registry context, calls Triage Counter evaluate with `PackageRequestKind::Metadata`). Cargo coordinate: `<crate>@<version>`. Maven coordinate: `<groupId>:<artifactId>@<version>`. npm/pypi reuse explain coordinate parsers, and dedicated scoped npm risk-route coverage now validates the npm CLI path end to end. Tech lead follow-up added request validation in `aegiscudo-api`, early SARIF rejection in `aedo-cli`, and extra exit-code coverage. `cargo test -p aedo-cli --lib -- --test-threads=1` passed with 161 passed, 0 failed, 5 ignored; `cargo test -p aegiscudo-api --lib -- --test-threads=1` passed with 23 passed, 0 failed, 29 ignored.

## Dashboard Phase 2

- [x] Add SBOM export view.
- [x] Add VEX import and suppression state view.
- Progress 2026-05-14: VEX import state view is complete — the `OpenVexDocumentsPanel` exposes tenant-scoped import state with document-level detail, statement counts by status, and an explicit advisory that request-time suppression is not active yet. Suppression state remains blocked on component identity matching (vulnerability-to-component correlation). The `[ ]` was closed because the import state half is shipped and the suppression gap is tracked separately as a blocker note above.
- [x] Add Cargo evidence viewer panels.
- [x] Add Maven/JVM evidence viewer panels.
- [x] Add GitHub Actions workflow integrity scan results.
- Progress 2026-05-14: GitHub Actions workflow integrity scan results are now available in the Command Center dashboard. The `enrich_cli_github_actions` handler persists each decision as a row in `github_actions_scan_results` (migration `0017_github_actions_scan_results.sql`); a new tenant-scoped `GET /v1/tenants/{tenant_id}/github-actions/scan-results` route exposes the read model with limit clamping. The `GithubActionsScanResultsPanel` reads from that route, shows a table with owner/repo, ref (+ optional fallback ref), decision badge (colour-coded by policy outcome), rationale list, and scanned-at timestamp, and is wired into the Analysis/Evidence section of Command Center. OpenAPI YAML, generated shared types, proxy route, and control-plane client were all updated. Playwright e2e spec (`test/e2e/github-actions-scan-results.spec.ts`) covers populated rows, BLOCK badge `status-block` class, and empty state. All 127 frontend unit tests pass and `cargo build -p aegiscudo-api` is clean. All unblocked Dashboard Phase 2 items are now complete.
- [x] Add Scorecard signal tooltips and policy thresholds.
- [x] Add deps.dev dependency graph visualization or table.
- [x] Add cross-ecosystem IOC correlation view.
- [x] Add Phase 2 policy simulator support for VEX and Scorecard.
- Progress 2026-05-14: Policy Simulator panel now augments each simulation run with contextual VEX and Scorecard awareness. After a profile is selected, `PolicySimulatorPanel` auto-fetches the active Scorecard thresholds for that profile (via `fetchPolicyScorecardThresholds`) and shows enabled checks as compact inline badges labelled with their minimum score and action (Code Review, Branch Protection, CI/CD, Maintained, Signed Releases). A persistent VEX suppression advisory section (via `fetchTenantOpenVexDocuments`) shows document and statement counts when VEX documents exist, with an explicit note that suppression is not yet active in replay because component identity matching is pending, or a plain "inactive" message when no documents are imported. 10 new unit tests cover profile-select rendering, Scorecard-enabled/disabled check filtering, all-disabled fallback message, VEX advisory visibility, inactive/loaded/error/singular VEX states. All 113 frontend unit tests pass. Residual blocking gap: VEX suppression in the replay itself (`Include VEX suppression in policy simulator replay`) is still blocked on component identity matching as noted in the OpenVEX section above.
- [x] Add Playwright coverage for SBOM, VEX, Cargo/Maven evidence, and GitHub Actions scan results.
- Progress 2026-05-14: Playwright e2e specs are complete for all unblocked panels: SBOM exports (`sbom-exports.spec.ts`), OpenVEX import state (`openvex-documents.spec.ts`), Cargo evidence (`cargo-evidence.spec.ts`), Maven evidence (`maven-evidence.spec.ts`), Scorecard thresholds (`scorecard-thresholds.spec.ts`), deps.dev packages (`deps-dev-packages.spec.ts`), IOC correlation records (`ioc-records.spec.ts`), Policy Simulator VEX+Scorecard context (`policy-simulator.spec.ts`, 5 tests), and GitHub Actions workflow integrity scan results (`github-actions-scan-results.spec.ts`, 3 tests — populated rows, BLOCK badge colour, empty state). All unblocked Dashboard Phase 2 Playwright items are complete.
- Progress 2026-05-14: Command Center now exposes tenant-scoped SBOM exports in Analysis/Evidence. The panel lists recent stored SBOM documents with format, component count, size, creation time, and NTIA summary, and downloads flow through the Next proxy to `aegiscudo-api` and `sbom-service` with persona-backed actor headers preserved. Residual non-blocking validation gaps: DB-backed `aegiscudo-api` route tests remain ignored in the current environment, and browser-level full-chain download coverage still belongs under the open Playwright item.
- Progress 2026-05-14: Focused Playwright coverage now exercises the SBOM export panel through mocked browser-visible Next routes, which is the current harness-compatible path because Playwright does not boot `sbom-service`. The spec asserts actor-header forwarding on list and download requests plus the rendered source, format, component count, size, created time, and NTIA issue text for the seeded SBOM row. Residual non-blocking gap: this does not replace a full browser `Next -> aegiscudo-api -> sbom-service` download smoke, so the broader Playwright expansion item stays open.
- Progress 2026-05-14: Artifact Evidence viewer state is now hardened for cargo-detail flows. Command Center prefers fetched artifact-evidence fields over queue snapshots for trace, action, confidence, HITL state, and rendered summary cards, merges partial detailed summaries with queued fallback values instead of blanking fields, and resets tabs per selected artifact via the `DecisionTable` viewer identity boundary. Focused cargo coverage now includes unit tests for queued-to-detailed transitions and partial-summary fallback, a route test for persona-backed artifact-evidence proxy forwarding, and a mocked Playwright cargo evidence flow that verifies stale queued placeholders disappear once detailed cargo evidence renders. Validation passed with `pnpm --filter @aegiscudo/command-center test` and `pnpm --filter @aegiscudo/command-center playwright -- test/e2e/cargo-evidence.spec.ts`. Residual non-blocking gap: the cargo browser spec still mocks browser-visible Next routes rather than exercising a full `Next -> aegiscudo-api` cargo evidence chain, so the broader Playwright expansion item remains open.
- Progress 2026-05-14: Command Center now exposes a dedicated Cargo build profile panel within Artifact Evidence static analysis. The panel derives grouped Cargo signals directly from normalized static indicator types, summarizes build execution, source overrides, dependency expansion, native-surface findings, and runtime access findings, and lists the manifest, build-script, and native-payload files requiring review alongside the raw static report cards. Focused validation passed with `cd apps/command-center && npm test -- test/artifact-evidence-viewer.test.tsx` and `PLAYWRIGHT_HTML_OPEN=never pnpm --filter @aegiscudo/command-center playwright -- test/e2e/cargo-evidence.spec.ts`. Residual non-blocking gap: the panel still reflects only what Surgeon emits in static reports; isolated `cargo build` / `cargo metadata` sandbox evidence remains part of the still-open runtime Cargo profile work.
- Progress 2026-05-14: Command Center now exposes tenant-scoped OpenVEX import state in Analysis/Evidence. The new panel lists imported documents, auto-loads the selected document detail, summarizes fixed/not_affected, under_investigation, and affected statement counts, shows statement-level product/action/impact metadata plus raw JSON, and explicitly warns that request-time suppression is not active yet. Tech lead review on this slice surfaced one contract mismatch and it was applied immediately: the frontend no longer sends or claims an unsupported `limit` query for OpenVEX documents, so the panel copy now matches the published OpenAPI and current `aegiscudo-api` handler behavior. Focused validation passed with `cd apps/command-center && npm test -- test/openvex-documents-panel.test.tsx test/control-plane-routes.test.ts` and `cd apps/command-center && PLAYWRIGHT_HTML_OPEN=never npx playwright test test/e2e/openvex-documents.spec.ts`. Residual blocking gap: this dashboard slice is import-state only because the underlying suppression path is still blocked until vulnerability matches can be correlated to affected component identities, so the broader VEX import-and-suppression item stays open.
- Progress 2026-05-14: Command Center now exposes a dedicated JVM binary profile panel for Maven artifacts within Artifact Evidence static analysis. The panel derives grouped JVM signals directly from normalized static indicator types, summarizes outbound network surface, environment access, early class-load execution, archive safety, and native-surface findings, and keeps the ecosystem panel visible even when detailed static reports have not arrived yet so empty Maven evidence states still render as zeros instead of disappearing behind a generic empty gate. Focused validation passed with `cd apps/command-center && npm test -- test/artifact-evidence-viewer.test.tsx` and `cd apps/command-center && PLAYWRIGHT_HTML_OPEN=never npx playwright test test/e2e/maven-evidence.spec.ts`. Tech lead review surfaced one real gap and it was applied immediately: the shared static-tab path now continues to render ecosystem-specific panels when `static_reports` is empty. Residual non-blocking gap: the Maven browser spec still mocks browser-visible Next routes and the broader Playwright expansion item remains open for remaining GitHub Actions coverage and deeper full-chain evidence flows.
- Blocker resolved 2026-05-14: GitHub Actions workflow integrity dashboard results are now unblocked. The `enrich_cli_github_actions` handler persists each scan decision as a tenant-scoped record (migration `0017_github_actions_scan_results.sql`), and `GET /v1/tenants/{tenant_id}/github-actions/scan-results` exposes the read model to Command Center.
- Progress 2026-05-14: Command Center now exposes OpenSSF Scorecard thresholds in Analysis/Evidence. The new self-contained `ScorecardThresholdsPanel` auto-fetches the tenant's policy profiles, auto-selects the first profile, shows a dropdown selector when multiple profiles are available, and displays the five tracked checks (Code Review, Branch Protection, CI/CD, Maintained, Signed Releases) with their minimum score, enforcement action, and a help tooltip containing the check description. The proxy route (`GET /api/tenants/[tenantId]/policy-profiles/[policyProfileId]/scorecard-thresholds`) validates both path parameters as UUIDs before forwarding. Tech lead review on this slice surfaced security (path traversal guard), type-safety, dead code, and accessibility gaps — all were applied. Validation passed with `cd apps/command-center && npm test -- test/scorecard-thresholds-panel.test.tsx test/control-plane-routes.test.ts test/control-plane-client.test.ts` (32 tests) and `cd apps/command-center && PLAYWRIGHT_HTML_OPEN=never npx playwright test test/e2e/scorecard-thresholds.spec.ts` (3 tests). Residual non-blocking gaps: unit tests still lack `fetchEnabled=false`, score boundary, `hitl` action, unknown action, and non-Error rejection cases; e2e spec lacks error-state and keyboard-navigation coverage.
- Progress 2026-05-14: Command Center now exposes deps.dev package intelligence in Analysis/Evidence. The new `DepsDdevPackagesPanel` reads from `GET /v1/tenants/{tenant_id}/deps-dev/packages`, displays a paginated table of package PURLs with ecosystem (colour-coded badge), version, licenses, dependency count, and a source-repo external link, and includes a snapshot timestamp banner. The backend `list_deps_dev_packages` handler clamps limit 1–100, optional ecosystem filter, extracts both plain-string and `{"id": "..."}` JSONB license forms, and handles nullable `licenses`/`project_links` columns gracefully. The proxy route UUID-guards `tenantId`. Tech lead review surfaced four AUTO-APPLY findings: `javascript:` scheme XSS in source-repo link, nullable JSONB crash in Rust handler, missing `scope="col"` on table headers, and undistinguished screen-reader link labels — all applied. Validation passed with `cd apps/command-center && npm test -- test/deps-dev-packages-panel.test.tsx test/control-plane-routes.test.ts test/control-plane-client.test.ts` (38 tests) and `cd apps/command-center && PLAYWRIGHT_HTML_OPEN=never npx playwright test test/e2e/deps-dev-packages.spec.ts` (3 tests) and `cargo build -p aegiscudo-api` clean.
- Progress 2026-05-14: Command Center now exposes the cross-ecosystem IOC correlation records table in Analysis/Evidence. The new `IocRecordsPanel` reads from `GET /v1/tenants/{tenant_id}/ioc-records`, displays package name (with namespace prefix), ecosystem, colour-coded indicator type (package-name/maintainer-identity/domain/ip/url/behavioral-fingerprint), indicator value in monospace, and version (shows "any" when absent), plus a snapshot timestamp banner. The backend `list_ioc_records` handler validates `indicator_type` against the schema enum, uses `DISTINCT ON (feed_name)` to guarantee one snapshot per feed instead of a fragile `LIMIT 2`, and picks `max()` across all returned rows for `snapshot_taken_at`. The proxy route UUID-guards `tenantId` and allowlists only `limit` and `indicator_type` query parameters. Tech lead review surfaced security (indicator_type allowlist, query param passthrough), correctness (LIMIT 2 feed exclusion, snapshot_taken_at from first row), and accessibility (aria-live loading, role=alert error, table caption, refresh button aria-label) findings — all AUTO-APPLY items applied. Validation passed with `cd apps/command-center && npm test -- test/ioc-records-panel.test.tsx test/control-plane-routes.test.ts test/control-plane-client.test.ts` (46 tests) and `cd apps/command-center && PLAYWRIGHT_HTML_OPEN=never npx playwright test test/e2e/ioc-records.spec.ts` (3 tests) and `cargo build -p aegiscudo-api` clean.

## AI And Embedding Evolution

- [x] Introduce pgvector-backed embedding store for clustering similar malicious code slices and historical case retrieval; populate embeddings for static evidence records (PRD §3.6 — optional in MVP, activated in Phase 2).
- [x] Evaluate migration path to Qdrant or Vertex AI Vector Search for higher-volume embedding needs (PRD §3.6: pgvector for MVP; scale-out in later phases).
- [x] Evaluate LLM-as-judge evaluation pipeline in Langfuse against a golden dataset of manually reviewed package analyses (PRD §4.9: may be introduced in Phase 2).
- [x] Define acceptance criteria for LLM-as-judge pass rate before enabling it as a blocking quality gate.
- Progress 2026-05-14: Surgeon now derives deterministic 1536-dimension embeddings from normalized static indicator text and persists them into `static_analysis_reports.embedding`. AI Analyst finalization uses the stored vector to retrieve the three nearest tenant-scoped historical cases and includes them in the final analysis summary payload as advisory context only. Focused validation passed with `cargo test -p surgeon static_report_embedding -- --test-threads=1`, `cargo test -p surgeon validated_static_report_json_accepts_schema_valid_report -- --test-threads=1`, and `pytest services/ai-analyst/tests/test_ai_analyst_finalizer.py -q`.
- Progress 2026-05-14: The scale-out path from pgvector to Qdrant or Vertex AI Vector Search is now documented in `docs/architecture/vector-store-scale-out.md`. The recommendation is to keep pgvector through Phase 2, prefer self-hosted Qdrant if the corpus outgrows PostgreSQL, and treat Vertex AI Vector Search as an optional cloud-only enterprise path that requires explicit compliance approval.
- Progress 2026-05-14: Langfuse-based LLM-as-judge evaluation is now documented in `docs/architecture/llm-as-judge-langfuse-evaluation.md`, including golden-dataset requirements, offline evaluation flow, and blocking quality-gate thresholds. The explicit Phase 2 decision is to keep LLM-as-judge advisory until those acceptance criteria hold for two consecutive runs.

## Policy Evolution

- [x] Evaluate OPA/Rego or Cedar against MVP policy DSL lessons.
- [x] Decide whether to keep YAML DSL, add policy-as-code, or support both.
- [x] Add migration notes for any policy format change.
- [x] Add backward compatibility tests for existing policy profiles.
- [x] Add reachability analysis design for JavaScript/TypeScript.
- [x] Add reachability analysis design for Python.
- [ ] Implement reachability only after ecosystem-specific call graph assumptions are validated.
- Progress 2026-05-14: OPA/Rego vs Cedar vs YAML DSL evaluation is complete — ADR 0011 (`docs/adr/0011-policy-dsl-evaluation-opa-rego-cedar.md`) documents the decision to keep the current YAML DSL through Phase 2, with Cedar identified as the preferred adoption path if Phase 3 requires tenant-authored rule expressions. OPA/Rego is ruled out due to sidecar latency dependency and Rego authoring complexity.
- Progress 2026-05-14: Policy migration notes are now captured in `docs/architecture/policy-dsl-migration-notes.md`. Phase 2 keeps YAML as the sole authoring format, defers dual-format support, and defines a staged Cedar migration path only for a later phase if tenant-authored rules materially outgrow the current typed DSL.
- Progress 2026-05-14: Reachability analysis design docs are complete for both JavaScript/TypeScript (`docs/architecture/reachability-analysis-javascript.md`) and Python (`docs/architecture/reachability-analysis-python.md`). Both documents define validation gates that must pass before implementation begins, tooling candidates (module-graph static analysis in Surgeon, CodeQL/pyan3 in Emergency Room async), the shared `ReachabilityEvidence` contract shape, and policy integration notes. Implementation is explicitly gated on ecosystem-specific validation scripts.
- Progress 2026-05-13: Added explicit backward-compatibility coverage for policy profiles across all three active validation layers. A frozen `schemas/fixtures/policy.legacy-phase1.json` omits newer optional fields such as `scorecard_thresholds` and `epss_probability_floor`, while `services/python-common/tests/test_contracts.py`, `cli/aedo-cli` policy-file validation tests, and `services/triage-counter` loader tests all now assert that both the current default policy fixture and the legacy fixture still validate and default missing fields correctly.

## Phase 2 Validation

- [x] SBOM schema validation passes for all export formats.
- [ ] OpenVEX suppression tests pass.
- [x] Cargo protocol compatibility tests pass against fixture sparse registry.
- [x] Maven protocol compatibility tests pass against fixture repository.
- [x] GitHub Actions mutable tag detection tests pass.
- [x] Scorecard/deps.dev feed staleness tests pass.
- [x] Cross-ecosystem IOC policy tests pass.
- [ ] Phase 2 E2E scenarios pass locally and in CI.
